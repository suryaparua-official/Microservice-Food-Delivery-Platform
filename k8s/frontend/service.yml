apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: zestify
spec:
  selector:
    app: frontend-service
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
