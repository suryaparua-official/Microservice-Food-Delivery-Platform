apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: zestify
spec:
  selector:
    app: admin-service
  ports:
    - port: 5006
      targetPort: 5006
  type: ClusterIP
