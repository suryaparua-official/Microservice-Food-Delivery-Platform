apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: zestify
spec:
  selector:
    app: auth-service
  ports:
    - port: 5000
      targetPort: 5000
  type: ClusterIP
