apiVersion: v1
kind: Service
metadata:
  name: realtime-service
  namespace: zestify
spec:
  selector:
    app: realtime-service
  ports:
    - port: 5004
      targetPort: 5004
  type: ClusterIP
